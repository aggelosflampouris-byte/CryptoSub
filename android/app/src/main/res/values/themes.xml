<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Since we use Jetpack Compose, this just defines the window background and removes the Action Bar. -->
    <style name="Theme.PrivateMessenger" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
