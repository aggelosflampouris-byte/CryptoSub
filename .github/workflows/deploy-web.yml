# This workflow has been retired.
# The web app has been replaced by the native Tauri desktop application.
# See .github/workflows/build-desktop.yml for the new build pipeline.

name: "[RETIRED] Deploy Web App to GitHub Pages"

on:
  workflow_dispatch: # Only allow manual trigger, never auto-triggers

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is retired. Use build-desktop.yml instead."
